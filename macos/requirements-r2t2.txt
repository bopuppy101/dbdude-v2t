# Optional: R2T2 (NetEase Youdao Confucius4-R2T2) model support for macOS.
# Install with: bash setup.bash --with-r2t2
#
# mlx-audio runs the community MLX 4-bit conversion of Confucius4-R2T2
# (selcukkubur/Confucius4-R2T2-mlx-4bit) natively on Apple Silicon in
# offline (whole-clip) mode. Model weights download from Hugging Face on
# first use under NetEase's Model Use License.

mlx-audio>=0.3.1
